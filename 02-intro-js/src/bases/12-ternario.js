const activo = true

/* let mensaje = '';

if (activo) {
    console.log('activo');
}else{
    console.log('Inactivo');
} */

/* const mensaje = (activo) ? 'Activo' : 'inactivo' */
const mensaje = activo && 'Activo'

console.log(mensaje);